//list of all the inums
// Note that they can not be created in the class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delived }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  credirCard,
  payStark,
  razorPay,
  paytm
}
